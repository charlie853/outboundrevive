# ─── Supabase ────────────────────────────────────────────────────────────────
SUPABASE_SERVICE_ROLE_KEY=*** set on server only ***
NEXT_PUBLIC_SUPABASE_URL=<same as above, allows it to be publically seen>
NEXT_PUBLIC_SUPABASE_ANON_KEY=<same as above, allows it to be publically seen>

# ─── Nango ──────────────────────────────────────────────────────────────────
NANGO_HOST=https://api.nango.dev
NANGO_SECRET_KEY=*** set on server only ***

# ─── Twilio ─────────────────────────────────────────────────────────────────
TWILIO_ACCOUNT_SID=AC_example_not_real          # placeholder; not a real SID pattern
TWILIO_AUTH_TOKEN=*** set on server only ***
TWILIO_MESSAGING_SERVICE_SID=MG_example_not_real

# Dry-run locally (no real SMS). Set to 0 in production.
TWILIO_DISABLE=1

# ─── App ────────────────────────────────────────────────────────────────────
PUBLIC_BASE_URL=http://localhost:3001

# Admin token for protected endpoints (used in x-admin-token header). Use a long random string.
ADMIN_TOKEN=dev-admin-token-change-me

# ─── Optional/legacy (not strictly required with DB settings) ───────────────
TIMEZONE=America/New_York
QUIET_START=09:00
QUIET_END=19:00
CAL_BOOKING_URL="https://cal.com/your-link"

# Notes:
# - Do NOT put real secrets in this file. Use .env.local for real values.
# - Keep .env.local out of git (see .gitignore).